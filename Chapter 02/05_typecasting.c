#include <stdio.h>
int main()
{
    int n = 45;
    float m = 32.3242;
    n = (int)m; // make m a interger
    // this is type casting, it coverts a data type into another.
    printf("%d", n);
    return 0;
}