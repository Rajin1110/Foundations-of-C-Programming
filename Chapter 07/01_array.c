#include <stdio.h>
int main()
{
    int marks[90];
    // starts with 0
    marks[0] = 45;
    marks[1] = 90;
    // we can go all the way till marks [89]
    printf("Marks 0 and marks 1 is %d %d\n", marks[0], marks[1]);
    return 0;
}