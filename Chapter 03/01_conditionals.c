#include <stdio.h>
int main()
{
    int age = 4;
    if (age > 5)
    {
        printf("your age is greater than 10");
    }
    else
    {
        printf("your age is lesser than 10");
    }
    return 0;
}
// relation operators
// == equals
// != not equal to
// >= greater than or equal to
// > greater than
